const assert = chai.assert;
describe('action controller', () => {

  it('allows for self-inflicted wounds', () => {
    const a = 3;
    assert.equal(a, 3);
  });

  it('allows for bear-inflictewd wounds', () => {
    const b = 3;
    assert.equal(b, 3);
  });


});